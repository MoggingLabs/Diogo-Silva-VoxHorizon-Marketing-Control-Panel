name: ci

# Pre-merge gate. Runs on every PR into main, plus on push to main as a
# post-merge final guard. Deploy is owned by `deploy-stack.yml` — this file
# never deploys anything.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref. Saves CI minutes when you push a
# follow-up commit before the previous build completes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Next.js app: typecheck, lint, unit tests, and a full production build to
  # catch any runtime/build-time misconfiguration. No secrets required at this
  # stage — the build runs without Supabase env vars, and any code that needs
  # them at build time should pull lazily.
  # ---------------------------------------------------------------------------
  next:
    name: Next (typecheck + lint + unit + build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm tsc --noEmit

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test:unit

      - name: Build
        run: pnpm build
        env:
          # Skip Sentry/telemetry init in CI builds.
          NEXT_TELEMETRY_DISABLED: "1"

  # ---------------------------------------------------------------------------
  # Python worker: install via uv (matches local toolchain) and run pytest.
  # ---------------------------------------------------------------------------
  worker:
    name: Worker (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Install deps
        run: uv sync --frozen --extra dev

      - name: Run tests
        run: uv run pytest -q

  # ---------------------------------------------------------------------------
  # Workflow YAML lint — catches typos / misconfigured actions before merge.
  # Cheap, runs in seconds.
  # ---------------------------------------------------------------------------
  workflow-lint:
    name: Workflow lint (actionlint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run actionlint
        uses: rhysd/actionlint@v1.7.7
